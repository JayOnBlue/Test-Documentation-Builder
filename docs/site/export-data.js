window.__DOCS_EXPORT__ = {"siteName":"Order Management Documentation","categories":["Getting Started","Orders"],"pages":[{"filename":"index.html","title":"Documentation Overview","feature":"Overview","category":"Getting Started","description":"How this documentation is generated, and how to find your way around it.","html":"<h2 id=\"overview\">Overview</h2>\n<p>This documentation describes the sample Order Management feature in <code>force-app/</code>. It is not written by\nhand — it is generated automatically every time metadata changes are pushed to <code>main</code>, then published to\nthis GitHub Pages site.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>None. This page always exists as the landing page for business/use-case documentation.</li>\n</ul>\n<h2 id=\"steps-to-navigate\">Steps to Navigate</h2>\n<ol>\n<li>Use the tabs above to move between documentation categories.</li>\n<li>Use the sidebar on the left to jump to a specific feature within a category.</li>\n<li>Press <strong>/</strong> anywhere to search across every page.</li>\n<li>Use the <strong>Technical Docs</strong> link in the top bar to see the component-level view of the same codebase —\nclasses, objects, flows, and how they depend on each other.</li>\n</ol>\n<h2 id=\"validations-business-rules\">Validations &amp; Business Rules</h2>\n<ul>\n<li>None — this page is informational only.</li>\n</ul>\n<h2 id=\"related-features\">Related Features</h2>\n<ul>\n<li>See <strong>Orders → Order Lifecycle</strong> for the one real example feature in this demo.</li>\n</ul>\n"},{"filename":"orders--order-lifecycle.html","title":"Order Lifecycle","feature":"Order Lifecycle","category":"Orders","description":"How an order moves from Draft to Confirmed, and what happens automatically when it does.","html":"<h2 id=\"overview\">Overview</h2>\n<p>An Order tracks a customer’s purchase, made up of one or more Order Lines (products, quantities, and\nprices). Sales and fulfillment staff use the Order record to move a purchase from a working draft to a\nconfirmed sale, at which point the customer is notified automatically.</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>The Order must have at least one Order Line before it is confirmed.</li>\n<li>The customer’s email address must be set on the Order.</li>\n</ul>\n<h2 id=\"steps-to-navigate\">Steps to Navigate</h2>\n<ol>\n<li>Open the Order record and review its Order Lines related list.</li>\n<li>Change <strong>Status</strong> from <em>Draft</em> to <em>Confirmed</em> and save.</li>\n<li>The system marks every Order Line on the order as confirmed and emails the customer automatically —\nno further action is needed.</li>\n</ol>\n<h2 id=\"validations-business-rules\">Validations &amp; Business Rules</h2>\n<ul>\n<li>Changing Status to <em>Confirmed</em> triggers <code>OrderTrigger</code>, which calls <code>OrderTriggerHandler</code> →\n<code>OrderService.confirmOrders()</code>.</li>\n<li><code>OrderService.confirmOrders()</code> marks every related Order Line as confirmed, then queues a confirmation\nemail via <code>OrderNotifier</code> (an <code>@future</code> call, so the email is sent asynchronously).</li>\n<li>The <strong>Order Confirmation Email</strong> flow also listens for the same Status change as a second, declarative\npath — see the Technical Docs tab for the full dependency picture.</li>\n</ul>\n<h2 id=\"related-features\">Related Features</h2>\n<ul>\n<li>See <strong>Technical Docs → OrderService</strong> for the exact method-level behavior and everything that depends on it.</li>\n</ul>\n"}]};