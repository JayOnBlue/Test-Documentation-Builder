<?xml version="1.0" encoding="UTF-8"?>
<Flow xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>60.0</apiVersion>
    <label>Contract Renewal Reminder</label>
    <processType>AutoLaunchedFlow</processType>
    <status>Active</status>
    <interviewLabel>Contract Renewal Reminder {!$Flow.CurrentDateTime}</interviewLabel>
    <start>
        <object>VendorContract__c</object>
        <recordTriggerType>Update</recordTriggerType>
        <triggerType>RecordAfterSave</triggerType>
        <filterFormula>DATEVALUE({!$Record.End_Date__c}) <= DATEVALUE(NOW()) + 30</filterFormula>
    </start>
</Flow>
